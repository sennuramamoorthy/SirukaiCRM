# Exclude host node_modules — CRITICAL for better-sqlite3 native module
# macOS-compiled Mach-O binaries will fail to load on Linux Alpine containers
node_modules
server/node_modules
client/node_modules

# Exclude compiled output (rebuilt inside container)
server/dist
client/dist

# Exclude database files (persisted via Docker named volume)
server/data
*.db
*.db-wal
*.db-shm

# Exclude env files (injected at runtime via env_file in docker-compose.yml)
.env
.env.*
server/.env

# Exclude git and editor files
.git
.gitignore
.DS_Store
*.swp
*.swo

# Exclude logs
npm-debug.log*
*.log
